# Lenscan only — create a separate Supabase project (do not reuse EventX).
# Project Settings → API: Project URL and anon (public) key.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

NEXT_PUBLIC_SUI_RPC_URL=

# Optional — Birdeye fallback for token prices not on DefiLlama (server-side only).
BIRDEYE_API_KEY=
